#!/usr/bin/env python3
# encoding: utf-8
"""
Конфигурация PID регулятора стабилизации корпуса.
Единая конфигурация используется для покоя и ходьбы.
"""

# ========== ЕДИНАЯ КОНФИГУРАЦИЯ PID РЕГУЛЯТОРА ==========
# Используется для стабилизации корпуса как в покое, так и при ходьбе
# Всегда пытается удержать корпус вертикально, независимо от режима
UNIFIED_PID_CONFIG = {
    # Включение/выключение PID стабилизации
    'pid_enabled': True,  # Глобальное включение/выключение PID
    'roll_enabled': True,  # Включить/выключить стабилизацию по roll
    'pitch_enabled': True,  # Включить/выключить стабилизацию по pitch
    
    # PID коэффициенты для roll
    'roll_kp': 0.00005,       # Пропорциональный коэффициент (м/градус)
    'roll_ki': 0.00005,      # Интегральный коэффициент
    'roll_kd': 0.00008,      # Дифференциальный коэффициент
    'roll_max_output': 0.02,  # Максимальное смещение по Y (м)
    'roll_target': 0.0,    # Целевой угол roll (градусы) - вертикально
    'roll_integral_limit': 10.0,  # Лимит интегральной составляющей (anti-windup)
    
    # PID коэффициенты для pitch
    'pitch_kp': 0.00005,       # Пропорциональный коэффициент (м/градус)
    'pitch_ki': 0.00005,      # Интегральный коэффициент
    'pitch_kd': 0.00008,     # Дифференциальный коэффициент
    'pitch_max_output': 0.02,  # Максимальное смещение по X (м)
    'pitch_target': 90.0,  # Целевой угол pitch (градусы) - вертикально
    'pitch_integral_limit': 10.0,  # Лимит интегральной составляющей (anti-windup)
    
    # Пороги активации (не реагируем на мелкие отклонения)
    'roll_activation_threshold': 2.0,   # Минимальное отклонение для активации PID (градусы)
    'pitch_activation_threshold': 2.0,  # Минимальное отклонение для активации PID (градусы)
    
    # Базовые смещения позы (начальные значения)
    'base_x_offset': 0.0,      # Базовое смещение по X (м)
    'base_y_offset': 0.0,     # Базовое смещение по Y (м)
    'base_roll_offset': 0.0,  # Базовое смещение по roll (градусы)
    'base_pitch_offset': 0.0, # Базовое смещение по pitch (градусы)
    
    # Пределы смещений от регулятора (безопасные пределы)
    'limit_x_offset_min': -0.03,
    'limit_x_offset_max': 0.03,
    'limit_y_offset_min': -0.03,
    'limit_y_offset_max': 0.03,
    'limit_roll_offset_min': -5.0,
    'limit_roll_offset_max': 5.0,
    'limit_pitch_offset_min': -5.0,
    'limit_pitch_offset_max': 5.0,
    
    # Коэффициенты для преобразования PID выхода в смещения туловища
    'roll_to_y_offset_ratio': 1.0,      # Коэффициент для init_y_offset от roll PID
    'roll_to_roll_offset_ratio': 0.5,  # Коэффициент для init_roll_offset от roll PID
    'pitch_to_x_offset_ratio': 1.0,    # Коэффициент для init_x_offset от pitch PID
    'pitch_to_pitch_offset_ratio': 0.5, # Коэффициент для init_pitch_offset от pitch PID
    
    # Настройки фильтра IMU
    'filter_enabled': True,
    'filter_alpha': 0.7,  # Коэффициент фильтрации (0-1), больше = меньше фильтрации
    
    # Интервал обновления PID
    'update_interval': 0.05,  # Интервал обновления PID корректировок (сек) - 20Hz для плавности
}

